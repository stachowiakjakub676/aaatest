{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Main window: web client only; no filesystem or shell access beyond the optional sidecar.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
